// initialize plug-ins, preload or eager load
$(function () {
    $('#fullpage').fullpage({
        // set the colour of each section for testing
        sectionsColor: ['#f00', '#fff', '#ff0', '#fff', '#f0f'],
        // navigation dots on the right
        navigation: true,
    });
})